/**
 * BUTTON STYLE GUIDE
 * 
 * PRIMARY: SpaceBtn with white text prop
 *   <SpaceBtn onClick={handleClick} white>Text</SpaceBtn>
 *   Use for: Main CTAs, important actions
 * 
 * SECONDARY: Button component (default)
 *   <Button onClick={handleClick}>Text</Button>
 *   Use for: Navigation, secondary actions
 * 
 * DESTRUCTIVE: Button with red styling
 *   <Button onClick={handleDelete} className="text-red-400 hover:text-red-300">
 *     Delete
 *   </Button>
 *   Use for: Delete, remove, dangerous actions (always with confirmation)
 */
